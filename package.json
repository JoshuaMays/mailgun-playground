{
  "name": "mailgun-playground",
  "version": "1.0.0",
  "scripts": {
    "client": "cd client && yarn start",
    "server": "nodemon server.js",
    "dev": "concurrently --kill-others-on-fail \"yarn server\" \"yarn client\""
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "dotenv": "^6.1.0",
    "express": "^4.16.2",
    "mailgun-js": "^0.21.0"
  },
  "devDependencies": {
    "concurrently": "^3.5.0"
  }
}
